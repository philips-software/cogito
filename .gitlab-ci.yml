image: node:latest

stages:
  - test
  - build

before_script:
  - yarn install

test javascript:
  stage: test
  script:
    - yarn lerna run --scope "@philips/**" --scope "@cogito/**" build
    - yarn test

test cogito-ios-app:
  stage: test
  variables:
    BUNDLE_GEMFILE: .ruby-dependencies/Gemfile
  before_script:
    - cd workspaces/cogito-ios-app
    - bundle install
  script:
    - bundle exec fastlane tests
    - bundle exec slather coverage
  tags:
    - ios

test telepath-ios:
  stage: test
  variables:
    BUNDLE_GEMFILE: .ruby-dependencies/Gemfile
  before_script:
    - cd workspaces/telepath-ios
    - bundle install
  script:
    - bundle exec fastlane tests
    - bundle exec slather coverage
  tags:
    - ios

build cogito-web3:
  stage: build
  script:
    - yarn lerna run --scope @cogito/cogito-web3 build
  cache:
    paths:
      - node_modules/
      - workspaces/cogito-web3/node_modules/

build telepath-js:
  stage: build
  script:
    - yarn lerna run --scope @cogito/telepath-js build
  cache:
    paths:
      - node_modules/
      - workspaces/telepath-js/node_modules/
